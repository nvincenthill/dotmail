import styled from 'styled-components';

const FormContainer = styled.form`
  max-width: 40rem;
  padding: 1rem 1.5rem;
  background: #f4f7f8;
  margin: 1rem auto;
  border-radius: 8px;
  font-family: Georgia, 'Times New Roman', Times, serif;
  box-shadow: 0.15rem 0.25rem 2rem -0.4rem rgba(0, 0, 0, 0.87);
`;

export default FormContainer;
